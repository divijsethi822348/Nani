<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="350dp"
    android:layout_height="wrap_content"
    android:background="@drawable/otp_popup_background">
    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/right"
        android:id="@+id/right"
        android:layout_marginTop="20dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/your_number_is_successfully_verified"
        android:id="@+id/number_verified_text"
        android:textColor="#000"
        android:fontFamily="@font/open_sans"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/right"
        android:layout_marginTop="20dp"
        android:singleLine="true"/>
    <Button
        android:layout_width="230dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/number_verified_text"
        android:layout_centerHorizontal="true"
        android:background="@drawable/orange_solid_button"
        android:textColor="@color/white"
        android:text="@string/verify"
        android:fontFamily="@font/open_sans"
        android:layout_marginTop="35dp"
        android:layout_marginBottom="40dp"
        android:id="@+id/verify_otp_popup"
        />


</RelativeLayout>